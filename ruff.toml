line-length = 120
target-version = "py311"

[lint]
select = ["E", "W", "F", "I", "UP"]
ignore = ["E501"]  # line length handled by formatter

[lint.isort]
combine-as-imports = true
known-first-party = ["hand_retarget", "hand_retarget_viz", "scene_builder"]
